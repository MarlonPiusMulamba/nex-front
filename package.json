{
  "name": "nex-front-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-client": "cd xclone-mobile-js && npm install",
    "build": "cd xclone-mobile-js && npm install && npm run build"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}